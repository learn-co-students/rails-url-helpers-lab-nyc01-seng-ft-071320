<div>
  <% @students.each do |student| %>
    <div><h1><%= student.first_name + " " + student.last_name %></h1></div>
    <div><%= link_to student.to_s, student_path(student) %></div>
  <% end %>
</div>