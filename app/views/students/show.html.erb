<h1><%= @student.to_s %></h1>
<% if @student.active %>
    This student is currently active.
<% else %>
    This student is currently inactive.
<% end %>
<h1><%= link_to "Activate", activate_student_path(@student) %></h1>
